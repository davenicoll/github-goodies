name: Labeler
on: 
- pull_request_target
- workflow_dispatch

jobs:
  label:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/labeler@v6.0.1
      with:
        repo-token: "${{ secrets.GITHUB_TOKEN }}"
